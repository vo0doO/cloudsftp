const electron = require('electron');
const exec = require('child_process').exec;
const spawn = require('child_process').spawn;
const appWindow = electron.remote.BrowserWindow.getFocusedWindow();
const Datastore = require('nedb');
const SSH = require('ssh2');
const https = require('https');
const ftp = require('@sftpapp/node-ftp');
const fs = require('fs');
const nodeBuffer = require('buffer').Buffer;
const os = require('os');
const fspath = require('path');
const tmp = require('tmp');
const util = require('util');
const crypto = require('crypto');
const buffersEqual = require('buffer-equal-constant-time');
const sshpk = require('sshpk');

const net = require('net');
const dns = require('dns');
const ping = require('ping');
const Traceroute = require('nodejs-traceroute');
const portscanner = require('portscanner');
const whois = require('whois');

const gcs = require('@google-cloud/storage');
const s3fs = require('s3fs');
const dfs = require('dropbox-fs/dist/index');
const backblazeb2 = require("node-backblaze-b2");
const webdav = require('webdav-fs');
